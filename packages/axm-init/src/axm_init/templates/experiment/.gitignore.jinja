# Caches and environments — never versioned.
__pycache__/
*.py[cod]
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.ipynb_checkpoints/
.DS_Store

# Everything else is EVIDENCE and stays versioned on purpose: the manifest and
# its lock, the freeze (git genealogy is what proves the pre-registration
# predates the measurement), the analysis, the figures and the readme.
