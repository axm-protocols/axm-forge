# Sources — {{ experiment_id }}

One row per input consumed by `scripts/`. An input with no row here is not an
input: it is an undeclared dependency.

| Input | Origin (URL / dataset / run id) | Retrieved on | Licence | Checksum |
|---|---|---|---|---|
| | | | | |

## Retrieval

Describe how each input is fetched (command, script, manual step) so the
`{{ reproduction_level }}` reproduction contract can be honoured by someone
who starts from an empty directory.
