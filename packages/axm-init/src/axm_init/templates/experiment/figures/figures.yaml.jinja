# Figures — {{ experiment_id }}
#
# The figure declaration read by the experiment contract. One entry per
# figure: a figure that is not declared here cannot be cited.
#
# Declare a figure by copying the skeleton below and uncommenting it:
#
# - id: fig-01-example                  # stable identifier, cited from the paper
#   caption: One sentence stating what the figure shows.
#   script: scripts/plot_example.py     # the script that renders it
#   reads: analysis/metrics.yaml        # the measurements it reads

[]
