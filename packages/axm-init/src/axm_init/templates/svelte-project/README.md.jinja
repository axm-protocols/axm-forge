# {{ package_name }}

{{ description }}

A SvelteKit app scaffolded by `axm init_scaffold --framework svelte`.

## Features

- SvelteKit + TypeScript (strict mode)
- ESLint (eslint-plugin-svelte) + Prettier + Vitest + svelte-check

## Installation

```sh
npm install
```

## Development

```sh
npm run dev    # Vite dev server
npm run lint   # ESLint (+ eslint-plugin-svelte)
npm run check  # svelte-check (type + a11y)
npm test       # Vitest
```

## License

{{ license }} © {{ author_name }}
