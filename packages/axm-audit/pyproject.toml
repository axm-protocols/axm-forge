[project]
name = "axm-audit"
dynamic = ["version"]
description = "Code auditing and quality rules for AXM"
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.12"
authors = [{ name = "AXM Protocols", email = "gabriel@axm-protocols.io" }]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Typing :: Typed",
    "License :: OSI Approved :: Apache Software License",
]
dependencies = [
    "axm",
    "axm-anvil",
    "axm-ast",
    "axm-echo",
    "axm-ingot",
    "complexipy==6.0.1",  # pinned: 5.6.0 panics on UTF-8 in noqa comments (rohaquinlop/complexipy#186); native dep run via uvx, lock does not protect — Dependabot proposes bumps
    "cyclopts>=4.21.0",
    "libcst>=1.5.0",
    "pydantic>=2.12.5",
    "radon>=6.0.1",
]

[project.scripts]
axm-audit = "axm_audit.cli:main"

[project.urls]
Homepage = "https://github.com/axm-protocols/axm-audit"
Documentation = "https://axm-protocols.github.io/axm-audit/"
Repository = "https://github.com/axm-protocols/axm-audit"
Issues = "https://github.com/axm-protocols/axm-audit/issues"

[project.entry-points."axm.tools"]
audit = "axm_audit.tools.audit:AuditTool"
audit_test = "axm_audit.tools.audit_test:AuditTestTool"
audit_fix = "axm_audit.tools.audit_fix:AuditFixTool"

[project.entry-points."axm.witnesses"]
audit_quality = "axm_audit.witnesses.audit_quality:AuditQualityRule"

[project.entry-points."axm.hooks"]
"audit:autofix" = "axm_audit.hooks.autofix:AutofixHook"
"audit:quality-check" = "axm_audit.hooks.quality_check:QualityCheckHook"

[project.entry-points."axm.commands"]
audit = "axm_audit.cli:app"

[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"


[tool.hatch.version]
source = "vcs"
tag-pattern = "audit/v(?P<version>.*)"
raw-options = { root = "../..", git_describe_command = "git describe --dirty --tags --long --match audit/v*" }
fallback-version = "0.1.0.dev0"

[tool.hatch.build.hooks.vcs]
version-file = "src/axm_audit/_version.py"

[tool.hatch.build.targets.wheel]
packages = ["src/axm_audit"]

[tool.hatch.build.targets.wheel.force-include]
"docs/test_quality.md" = "axm_audit/docs/test_quality.md"
"docs/fix_pipeline.md" = "axm_audit/docs/fix_pipeline.md"
"docs/index.md" = "axm_audit/docs/index.md"

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "pytest-cov>=7.1.0",
    "pytest-asyncio>=0.24",
    "pytest-json-report>=1.5",
    "pytest-mock>=3.15.1",
    "ruff==0.15.21",
    "mypy>=1.20.0",
    "prek>=0.4.10",
    "pip-audit>=2.0",
    "bandit>=1.9.3",
]
docs = [
    "mkdocs-gen-files>=0.6.0",
    "mkdocs-literate-nav>=0.6.3",
    "mkdocs-material>=9.0",
    "mkdocstrings[python]>=1.0.3",
]

# ─────────────────────────────────────────────────────────────────────────────
# Ruff Configuration
# ─────────────────────────────────────────────────────────────────────────────
[tool.ruff]
target-version = "py312"
line-length = 88
exclude = [
    "*/_version.py",
    "scripts/*",
    "tests/fixtures/fix_corpus/*/input",
    "tests/fixtures/fix_corpus/*/expected",
]

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "BLE", "C4", "C901", "PLR", "S", "T20", "RUF"]
ignore = ["S101"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["S101", "S105", "S106", "S108", "PLR2004"]
# Fix-corpus loaders intentionally invoke `git` and `axm-audit` by PATH name
# (subprocess controls all args; no untrusted input).
"tests/fixtures/fix_corpus/conftest.py" = ["S603", "S607"]
"tests/fixtures/fix_corpus/regenerate.py" = ["S603", "S607"]
"src/axm_audit/core/rules/quality_rules.py" = ["S603", "S607"]
"src/axm_audit/core/rules/dependencies.py" = ["S603", "S607"]
"src/axm_audit/cli.py" = ["T201", "PLR0913"]
"src/axm_audit/core/test_runner.py" = ["PLR0913"]
# core/fix/ is the relocated fix-proto: deterministic CST-based test reorganiser
# (12 modules, intentionally complex). libcst visitor methods (leave_*/visit_*)
# require PascalCase names (N802); CST rewriters routinely exceed cyclomatic /
# branch / statement / arg thresholds; sha1 is used for content fingerprints
# (not security); subprocess calls invoke ruff via local PATH (not untrusted
# input). Same rationale as core/test_runner.py for PLR0913.
"src/axm_audit/core/fix/*" = [
    "B007",
    "C901",
    "E501",
    "N802",
    "PLR0911",
    "PLR0912",
    "PLR0913",
    "PLR0915",
    "PLR1714",
    "PLR2004",
    "RUF005",
    "RUF022",
    "RUF034",
    "S324",
    "S603",
    "S607",
]
# Pydantic models intentionally use `Field(default_factory=...)` for mutable defaults.
"src/axm_audit/core/rules/test_quality/duplicate_tests.py" = ["RUF012"]
"src/axm_audit/core/rules/test_quality/pyramid_level.py" = ["RUF012"]
"src/axm_audit/core/rules/test_quality/tautology.py" = ["RUF012"]

[tool.ruff.lint.mccabe]
max-complexity = 10

[tool.ruff.lint.pylint]
max-args = 5
max-branches = 12
max-returns = 6
max-statements = 50
max-locals = 15

[tool.ruff.lint.isort]
known-first-party = ["axm_audit"]

# ─────────────────────────────────────────────────────────────────────────────
# Deptry Configuration
# ─────────────────────────────────────────────────────────────────────────────
[tool.deptry]
known_first_party = ["axm_audit"]
extend_exclude = ["scripts"]

[tool.deptry.per_rule_ignores]
DEP004 = ["mkdocs_gen_files"]

# ─────────────────────────────────────────────────────────────────────────────
# MyPy Configuration
# ─────────────────────────────────────────────────────────────────────────────
[tool.axm-audit.mirror]
# tools/audit.py is the `axm-audit audit` sub-command wrapper. Per the pyramid
# spec, the single-binary canonical e2e name strips the binary prefix, so
# `tests/e2e/test_audit.py` IS the canonical form — not an anti-mirror smell.
exempt_paths = [
    "**/__init__.py",
    "_version.py",
    "__main__.py",
    "tools/audit.py",
    # I/O-only collector (filesystem rglob + git subprocess, no pure logic);
    # its tests legitimately live in tests/integration/ — a unit mirror would
    # be mock-everything noise.
    "code_metrics.py",
]

# Glob-based exemptions (MirrorRule semantics, survive refactors). The
# `tests/fixtures/**` corpus subtree is excluded from dedup collection by
# default — no ack needed for the input/expected snapshot pairs.
[tool.axm-audit.duplicate_tests]
exempt_paths = []

# ── Legitimate intra-file residual (genuine same-SUT near-duplicates) ──
[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "dff54d3151e2"
reason = "Intra-file same-SUT residual in test_relpath.py: test_relpath_relativizes_inside_project and test_relpath_falls_back_to_absolute_when_outside both exercise relpath() but guard opposite branches (inside-project relativization vs outside-project absolute fallback); parametrizing would erase the branch-per-test documentation. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "bae3922cf705"
reason = "Tests cover orthogonal format_report concerns (pass/fail icons, category grouping, per-check score display, Improvements section presence/details/tip) with distinct assertion targets; collapsing would lose signal."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "de885c684c98"
reason = "Tests cover distinct contracts: 'Score:' header, project_path fallback to 'unknown', category=None grouping under 'other', and format_json required-key surface; each guards a separate format_report/format_json branch."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "0a7c6a5b3a39"
reason = "format_agent_text branches under test are distinct: passed-actionable rendering (✓ + rule_id), failed-no-text-falls-back-to-details path, and empty-input header counts (0 pass/0 fail); each guards a different rendering branch."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "6314b4ebf3f9"
reason = "Each test guards a distinct check-details renderer (QUALITY_SECURITY top_issues with severity/code/file/line vs DEPS_HYGIENE top_issues with code/module/message); the two schemas are unrelated and asserted on different rendered prefixes."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "231e97b89c9a"
reason = "Tests guard 4 distinct quality_score properties across different classes: parametrized range over every scored category (TestQualityScoreRange), single-category averaging behaviour (TestSingleCategoryAveraging), filtered-audit renormalisation invariant (TestFilteredAuditRenormalization), and an ignore-unknown-inputs branch; collapsing would erase the property each class documents."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "8049e70ad5cd"
reason = "Tests target different scoring surfaces: registry-derived all-100 invariant (TestRegistryDerivedScoring), registry-resilience to new rules (rule-addition guard), and public-API single-category renormalisation; each guards a distinct contract (registry coverage vs API behaviour) at different abstraction layers."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "c2d6c85495c6"
reason = "Tests assert different bound contracts: the first checks the absolute [0, 100] envelope from a heterogeneous LINT+TYPE pair, the second asserts the tighter [min(cat_avg), max(cat_avg)] = [60, 90] envelope when categories are combined; collapsing would lose the combine-vs-clamp distinction."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "d11f876f75c9"
reason = "Each test guards a distinct I/O-detection branch of PyramidLevelRule: import-then-called (R1 httpx call), stat-family attr (Path.is_file syscall), tmp_path.write_text boundary signal, and constructor-result-flow taint to method call ('fixture-arg:tmp_path'); they assert different io_signals strings and cannot be folded without losing the branch each documents."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "051a12ddc441"
reason = "Tests guard 3 disjoint classification branches: R2 pure-function unit path (asserts reason 'pure function'), R3 per-function attr:.write_text() signal, and the tmp_path+write/read boundary signal; each asserts a different io_signals string or 'reason' field, so they document independent classifier rules."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "9cbc773e0783"
reason = "Both tests assert level=='unit' negatively but guard orthogonal R1/R5 sub-rules: imported-but-unused module (httpx) must not produce the 'imports httpx' signal, while a string-literal receiver of .replace() must not trigger 'attr:.replace()'; the heuristics live in different code paths and the assertions probe different io_signals."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "e4797f11d445"
reason = "Tests assert opposite classifications (integration vs unit) on unrelated fixture/code-shape patterns: a conftest `fake_*` fixture that performs real I/O must NOT be mock-neutralised, while a tmp_path argument passed to a pydantic constructor inside pytest.raises must NOT be tainted; they exercise different sub-rules."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "3f38caa6fa2b"
reason = "Tests guard 3 disjoint tautology-triage steps with different action/rule pairs: step_n2_import_smoke yields DELETE, step_n1_no_siblings yields STRENGTHEN, and step0_marker_opt_out yields KEEP; the assertions probe distinct verdict branches and cannot be folded."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "83f58bf1906b"
reason = "Tests guard opposite verdicts on different inputs: a self-compare assertion yields STRENGTHEN+step0_self_compare, while an unclassified isinstance(x, object) yields UNKNOWN+step5_default_unknown; collapsing would erase the strengthened-vs-fallback boundary."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "e2edc9eb0fa1"
reason = "Tests assert opposite actions on Foo constructor siblings: distinct kwargs (name='a' vs name='b') yield STRENGTHEN+step1b_different_args, while identical n-copies (both name='x') yield DELETE+step0b_n_copies_constructor; the two paths exercise different triage rules."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "52b6ec3e7024"
reason = "Tests assert different rule_id membership surfaces: category_filter_includes_new_rules guards practices category contents (PRACTICE_BLOCKING_IO in, PRACTICE_LOGGING out, PRACTICE_DOCSTRING in), while all_rules_includes_tooling guards the all-rules path surfaces TOOL_RUFF/TOOL_MYPY/TOOL_UV; one probes filtering, the other probes auto-discovery."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "586c324779d9"
reason = "Cluster covers 3 distinct setup-divergence sub-cases of DuplicateTestsRule: signal1/signal3 module-level pair (no setUp), raises-symmetric pair (both-with vs neither-with pytest.raises), and same self.parser SUT after setUp; each asserts a different signal prefix or signal name and probes an independent clustering branch."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "7a4f2c51e702"
reason = "Tests assert different signal names on different inputs: cross_class_pair asserts signal=='ambiguous_distinct_class' for two TestFoo/TestBar bodies, while one_with_raises_one_without asserts signal=='ambiguous_raises_divergence' for an asymmetric pytest.raises pair; collapsing erases the distinct-class-vs-raises-divergence distinction."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "608caa63b77c"
reason = "Tests probe two distinct config-fallback branches of DuplicateTestsRule: no pyproject.toml at all vs pyproject without the [tool.axm-audit.duplicate_tests] section; both must not raise config_error but each exercises a separate filesystem/parsing path (file missing vs section missing)."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "57d28ca015b8"
reason = "Tests guard 3 disjoint glob semantics in MirrorRule.exempt_paths: 'commands/*.py' single-directory match (data.py in exempt), '**/_facade.py' nested-path match (recursive descent), and '*.py' single-star non-crossing slash (sub/foo.py stays in missing); each pins a different glob feature."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "ff7b20c24c43"
reason = "Tests probe 3 orthogonal MirrorRule branches: backwards-compatible no-section legacy behavior (foo.py in missing, exempt=[]), '?' single-char glob match (v1.py in exempt), and exempt module that has a test still allowed (missing=[] AND data.py in exempt); each asserts a different missing/exempt outcome."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "6d5d053e7ffd"
reason = "Cluster covers 7 disjoint criterion-(a) detection paths of test_references_first_party: direct first-party import+call, fixture return-annotation (Rule), fixture body returning Rule(), module-helper closure with first-party symbol, stdlib-only (negative), first-party-only-in-fixture-body closure extension, and unconsumed-fixture param-gating (negative); each probes a different resolver branch with True/False verdicts."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "037cd1d18781"
reason = "Cluster covers 6 disjoint criterion-(b) detection paths of test_invokes_in_package_script: subprocess.run with declared 'pkg-cli' script, 'python -m pkg_cli' module-path form (underscore alias), plumbing-only subprocess (git/uv, negative), CliRunner().invoke for single-binary packages, unresolved-argv permissiveness, and subprocess-only-in-fixture-body closure; each probes a different invocation detector branch."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "cadd568de417"
reason = "Tests guard 3 disjoint NoPackageSymbolRule branches: parametrized 'either-criterion passes' verdict (a-only/b-only/both bodies in tests/integration/), file-level pytest.mark.no_package_symbol_ok marker exclusion, and the tests/unit/ tier always being skipped; each probes a different rule path with no shared input/assertion shape."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "efdd6ffcf529"
reason = "Tests assert no-finding on two structurally different legitimate inputs: an e2e test invoking the declared CLI via subprocess (criterion b) and an integration test importing a first-party symbol (criterion a); each pins a different criterion path in NoPackageSymbolRule."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "eee3071fb748"
reason = "Tests guard two disjoint extract_imports branches: parametrized single-statement cases (plain import vs relative-import-no-module) asserting the resulting module list, and the __future__-only edge case asserting an empty list; the assertion shapes (parametrized expected list vs literal == []) are not foldable without erasing the __future__-directive-excluded contract."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "4a41a7e4f458"
reason = "Tests guard two disjoint strip_prefix branches: parametrized inputs (shared-prefix strips, no-dot returns unchanged, mixed-prefix returns unchanged) asserting full output equality, and the empty-list edge case asserting == [] directly; the shapes (parametrized expected list vs literal empty) are not foldable without losing the empty-input contract."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "711a9dd09b86"
reason = "Cluster covers 3 disjoint detect_first_party_packages branches with different setups: short-circuit when [tool.deptry] known_first_party is set (returns []), parametrized non-short-circuit cases (no-pyproject, no-deptry-section, malformed-toml — all return ['mypkg']), and the empty src/ directory edge case (returns []); each probes a different config-vs-filesystem detection path."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "62d416d20d18"
reason = "Tests guard 3 disjoint _report_to_result text-rendering branches of TestCoverageRule: failure-test-id with '::' separator (asserts FAIL last-segment 'test_bar'), failure-test-id without '::' (asserts FAIL unchanged 'test_simple'), and 100% coverage + 1 failure (asserts no 'cov' line, only FAIL line); each probes a different rendering path on different inputs."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "3e82563cc26a"
reason = "Tests guard two disjoint AuditQualityRule aggregation branches in distinct classes: TestAuditQualityLintFails fakes lint=2 fails + type=pass (asserts '2 violation(s)', verifies type still ran despite lint failure — no short-circuit on lint), TestAuditQualityBothFail fakes lint=1 + type=2 fails (asserts '3 violation(s)', cross-category aggregation); each pins a different no-short-circuit semantics with different mock side_effect shapes and cannot be folded."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "cc3f9a49d858"
reason = "Tests guard two disjoint ASTCache lifecycle branches inside TestUnitScope: test_default_is_none asserts get_ast_cache() is None at module init (no prior set, default ContextVar value), while test_clear asserts the same None after a set_ast_cache(ASTCache()) + set_ast_cache(None) round-trip (clearing semantics); the setup divergence is the contract being pinned and cannot be folded."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "225268402bde"
reason = "Cluster spans 3 disjoint read_coupling_config branches: overrides for an existing first-party module (rules.quality=20), overrides silently kept for a nonexistent module (no.such.module=25), and the unrelated severity_error_multiplier=3 field (different tuple slot); each pins a distinct assertion target (overrides dict membership vs multiplier int) and cannot be folded."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "d64d53ad6b2c"
reason = "Tests guard 3 disjoint AntiMirrorRule pass-branches: scenario-named tests (assert passed AND score==100 across integration+e2e dirs), unit-dir is never walked (AC13, unit/test_foo.py source-name collision must NOT flag), and exempt_paths config (commands/*.py glob via pyproject [tool.axm-audit.mirror].exempt_paths skips anti-mirror); each probes a different rule path with distinct setup (no config / unit dir only / pyproject config) and cannot be folded."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "7fa1b65783dd"
reason = "Cluster spans two distinct SUTs in test_test_runner: TestParseFailures.test_empty_list and test_passing_tests_skipped exercise parse_failures([]) / parse_failures([passed]), while TestParseCollectorErrors.test_empty_list exercises parse_collector_errors([]); both parsers are public API and each empty-list contract guards a different function — folding would erase the parse_failures vs parse_collector_errors boundary."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "b052a0b0f5ad"
reason = "Tests guard two disjoint radon-detection branches of ComplexityRule.check: radon_in_project exercises the API path with the real complexipy/radon imports available (no patches, asserts passed + score>=90), while radon_fallback patches _try_import_radon to None and shutil.which/subprocess.run to simulate the binary-only fallback path consuming a synthetic radon JSON payload; each pins a different acquisition-of-metrics branch."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "338c5b72e66e"
reason = "Tests assert opposite verdicts on DependencyAuditRule's env-tool filter: excludes_each_env_tool parametrizes over the known env-tool allowlist (pip/setuptools/wheel/uv/pip-audit, case-insensitive) and asserts vuln_count==0, while passes_through_unknown_package feeds 'numpy' (not in the allowlist) and asserts vuln_count==1; collapsing erases the include-vs-exclude classification boundary."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "d675e96daf1d"
reason = "Tests guard two disjoint DependencyHygieneRule text-rendering branches: single_package_text_format asserts multi-issue concatenation with a known code mapping (DEP002 -> 'unused dep', DEP001 -> 'missing dep' joined by '\\n'), while unknown_deptry_code feeds an unmapped 'DEP999' code and asserts the message passes through verbatim ('• DEP999 mod: some new rule'); collapsing erases the known-code-mapping-vs-unknown-passthrough distinction."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "75d5890ee84a"
reason = "Tests guard two disjoint ProjectRule.check_src layout branches that both return None: single_package builds tmp_path/src (asserts check_src yields None so the rule descends into a single src tree), while multi_package builds tmp_path/packages/{a,b}/src (asserts None so iter_src_dirs handles the multi-pkg fan-out); the divergent layout IS the pinned contract because each path routes the caller to a different walker and folding erases the single-vs-multi distinction."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "d1fd8b811e81"
reason = "Tests guard two disjoint defensive-parse branches of parse_file_safe: syntax_error writes invalid Python source ('def broken(\\n') triggering ast.parse SyntaxError, while binary_file writes non-UTF-8 bytes (b'\\x80\\x81\\x82\\x83') triggering a decode error before parsing; both return None but each exercises a different except clause (SyntaxError vs UnicodeDecodeError) and folding erases the input-shape-vs-exception-kind distinction."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "191d3b600d73"
reason = "Tests pin opposite ends of LintingRule's scoring curve: issues_reduce_score feeds two unused-import violations (import os, import sys) and asserts score<100 + issue_count>0, while lint_threshold_unchanged feeds one mild unused import and asserts the score still clears the 90 threshold (>=90) — the first guards the score-reduction monotonicity, the second guards the no-regression-on-mild-issues threshold contract; folding erases the strict<100-vs-loose>=90 boundary."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "4ac6b6be2637"
reason = "Inter-file cluster mixes pass-paths and detection-paths across 5 different rules (circular-import passes, duplication passes, mirror passes, typecheck passes, plus cycle/god-class DETECTION assertions). Heterogeneous concerns: positive vs negative verdicts on unrelated SUTs with no shared fixture or helper signature. Folding would require cross-rule parametrization that erases the pass-vs-detect axis."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "6a6792d55844"
reason = "Inter-file cluster mixes 5 unrelated rule integrations: CouplingMetricRule above-threshold case, CouplingMetricRule __init__.py exclusion, DependencyHygieneRule workspace-aggregate behavior, DependencyHygieneRule single-package preserved behavior, and a SecurityRule bandit RC1 reporting check. Each test owns its own pyproject/source layout and rule SUT; cross-rule folding would couple unrelated rule modules. Distinct-SUT — ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "f387adcf66a7"
reason = "Inter-file cluster spans 3 distinct AuditQualityRule failure paths: all-categories-raise (every audit_project call raises RuntimeError → 'All audit categories failed' feedback), type-fails (lint passes + 3 mypy failures aggregated → '3 violation(s)' feedback), and working-dir-invalid (nonexistent path → 'Not a directory' feedback before any audit runs). Each guards a different early-exit/aggregation branch with different mock setups and feedback assertions. No shared helper signature. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "40d3baeaaf2f"
reason = "Inter-file cluster covers 3 unrelated failure-mode rules: BareExceptRule (bare `except:` → bare_except_count>0), SecurityPatternRule (hardcoded password → secret_count>0), and TypeCheckRule (return type mismatch → error_count>0). Each asserts a different details key on a different rule with disjoint fixture content. No shared helper signature; folding would erase per-rule failure contracts. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "b1afc520af37"
reason = "Inter-file cluster covers 3 unrelated text-rendering checks: CircularImportRule preserves bare module names without dot-stripping (test_text_no_prefix_strip_for_bare_modules), and DuplicationRule emits bullet+arrow markers (test_duplicate_text_has_bullets) and includes file paths (test_duplicate_text_has_file_paths) for clone pairs. The two DuplicationRule tests assert disjoint text contents (•/→ glyph presence vs both a.py and b.py path presence) on the same fixture and the circular-import test guards an unrelated rule. No shared helper signature spanning the two files. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "fbc4ea5f345b"
reason = "Inter-file cluster spans 3 distinct scoring/passing branches: CouplingMetricRule mixed-severity scoring (2 warnings + 1 error → score=89 via 100 - (2*3 + 1*5)), CouplingMetricRule error-floor (25 modules × fan-out=25 → score=0 floored), and LintingRule clean-project (zero ruff issues → score=100). Different SUTs (coupling vs lint), different scoring formulas, different fixture shapes. No shared helper signature spanning the two files. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "7e2a43857cbe"
reason = "Inter-file cluster covers 3 unrelated severity/passed contracts: CouplingMetricRule mixed-severity returns Severity.ERROR + passed=False on a fakepkg with one warning + one error module, MirrorRule returns passed=True + Severity.INFO when src/ is absent (graceful skip), and ToolAvailabilityRule parametrizes severity by `critical` flag (False→WARNING, True→ERROR) on a missing tool. Three different rules, three different severity-derivation branches, disjoint inputs. No shared helper signature. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "8ea79996167a"
reason = "Inter-file cluster spans 3 unrelated rule SUTs: PyprojectCompletenessRule.test_complete_pyproject_passes (parametrized over static vs dynamic version, asserts passed=True + score=100 on a 9-field pyproject), PyprojectCompletenessRule.test_missing_pyproject_fails (asserts passed=False + score=0 when pyproject.toml is absent), and TypeCheckRule.test_typed_project_high_score (parametrized over greet/double, asserts passed=True + score=100 on a fully-typed src tree). Different rules (STRUCTURE_PYPROJECT vs QUALITY_TYPE), opposite outcomes (pass-100 vs fail-0), disjoint fixture shapes (pyproject content vs src/ layout). No shared helper signature spanning the two files. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "d1ac62c64bc6"
reason = "Inter-file cluster covers 3 distinct `passed-entry stays as string` branches of format_agent across different rule_id types: PRACTICE_DOCSTRING with no details payload (test_passed_clean_is_string asserts a QUALITY_TYPE check with score=100 and no details renders as string), PRACTICE_DOCSTRING with details={'missing': []} empty-missing case (test_passed_empty_missing_is_string asserts empty actionable list still folds to string), and QUALITY_COMPLEXITY with details={'top_offenders': []} empty-offenders case (test_passed_complexity_no_offenders_is_string asserts an unrelated rule's empty-actionable branch). Each guards a different rule_id's actionable-keys check inside the formatter's passed-rendering branch. No shared helper signature. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "90878e753845"
reason = "Tests guard two disjoint defensive branches of QualityCheckHook.execute that both yield `has_violations is False`: test_audit_crash (integration) mocks audit_project to raise RuntimeError('boom') and asserts the hook swallows the exception, while test_invalid_working_dir (unit) supplies an invalid working_dir path so the hook short-circuits before audit_project is ever called. Same negative postcondition but different code paths (exception-handling vs pre-check), different setup (mocker.patch vs raw bad path), different pyramid levels. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "e22bd2c1e679"
reason = "Canonical 'different-rule-class' inter-file case: CouplingMetricRule.test_single_segment_module_name asserts the bullet-format rendering for a dotless module name (`• utils fo:15/12 ⚠`) via monkeypatched _compute_coupling_metrics + _resolve_coupling_severity, while SecurityPatternRule.test_text_bullet_format asserts the bullet-format rendering for a hardcoded password match (`• bad.py:2 password`) on a real src file. Different SUTs (architecture vs security), different fixtures (monkeypatch vs real I/O), different text payload schema (`{leaf} fo:N/T {symbol}` vs `{file}:{line} {pattern}`). ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "d3e92de3fd36"
reason = "Canonical 'different-rule-class' inter-file case explicitly noted in the dedup-tests playbook: DirectoryExistsRule.test_directory_presence parametrizes (create_dir, passed, substr) on `(tmp_path/'src')` while FileExistsRule.test_file_presence parametrizes (create_file, passed, substr) on `(tmp_path/'README.md')`. Same parametrized shape but two different rule SUTs (structure.DirectoryExistsRule vs structure.FileExistsRule), different constructor kwargs (dir_name vs file_name), different filesystem object asserted (mkdir vs write_text). ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "e16257fc86ec"
reason = "Canonical 'different-rule-class' inter-file case (companion to d3e92de3fd36): DirectoryExistsRule.test_directory_exists_no_fix_hint asserts `fix_hint is None` on the passing-dir branch of DirectoryExistsRule, while FileExistsRule.test_file_exists_no_fix_hint asserts the same on the passing-file branch of FileExistsRule. Two different rule SUTs guarding the same no-fix-hint-when-passing contract on disjoint rule classes; folding would couple structure.DirectoryExistsRule and structure.FileExistsRule into a single parametrize and erase the per-rule contract. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "c91efcd42753"
reason = "Inter-file heterogeneous case: TautologyRule.test_severity_warning seeds `test_sample.py` with `assert True` and asserts severity == WARNING (tautology contract), while ToolAvailabilityRule.test_found_tool_has_info_severity instantiates the rule with `tool_name='python3'` and asserts severity == INFO (tool-availability contract). Different rule SUTs (test_quality.tautology vs tooling.ToolAvailabilityRule), different severities (WARNING vs INFO), different setup paths (filesystem seed vs in-memory rule construction). ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "c70a2fa2b83a"
reason = "Inter-file heterogeneous case: TypeCheckRule.test_type_check_path_unchanged parametrizes (path, expected_substring) over tests/scripts paths and asserts `path:` is preserved in rendered text via mocked mypy JSON, while DeadCodeRule.test_dead_code_text_path_fallbacks parametrizes (symbol_dict, expected_substring) over module_path/missing-file fallbacks and asserts the bullet rendering via mocked subprocess axm-ast. Different rule SUTs (TypeCheckRule vs DeadCodeRule), different fixtures (`_patch_infra` + `_mock_mypy` vs `mocker` + `subprocess.run` patch), different rendered schemas. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "d291aa9a22d1"
reason = "Inter-file heterogeneous case: ComplexityRule.test_all_offenders_shown generates 8 high-CC functions on-disk and asserts `top_offenders` includes ALL of them (no top-5 cap), while FormattingRule.test_many_unformatted_capped_at_20 mocks ruff format --check stdout with 30 unformatted-file lines and asserts the surfaced list is capped at 20. Different rule SUTs (complexity.ComplexityRule vs quality.FormattingRule), different contracts (no-cap-for-offenders vs cap-at-20-for-files), opposite cap semantics on disjoint payloads. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "9efbd8dbef78"
reason = "Inter-file heterogeneous case spanning 3 rule SUTs with disjoint details keys: DocstringCoverageRule.test_missing_no_cap asserts len(details['missing'])==15 (uncapped missing-docstring list), LintingRule.test_lint_issues_match_count asserts len(details['issues'])==min(issue_count,20) (cap-at-20 issues list), and TypeCheckRule.test_typecheck_errors_match_count asserts len(details['errors'])==error_count (uncapped errors list). Each rule exposes a different details field with a different cap contract on disjoint fixtures (15 funcs vs unused imports vs return-type mismatch). No shared helper signature. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "121e82ae611c"
reason = "Inter-file heterogeneous case: scan_package.test_known_true_e2e_files_stay_e2e runs scan_package(AXM_AUDIT) and asserts true-e2e files are NOT reclassified away from e2e (pyramid-level classifier contract), while test_base.test_promoted_symbols_not_in_root_all imports axm_audit and asserts the _PROMOTED helper set does not leak into axm_audit.__all__ (API-surface contract). Different SUTs (pyramid scanner vs package __all__), different fixtures (real AXM_AUDIT scan vs in-process import), unrelated 'leaked set is empty' assertions on disjoint concerns. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "7dbb1bb9c335"
reason = "Inter-file heterogeneous case both asserting passed+score==100 on two unrelated rules: DuplicationRule.test_no_src_directory runs check on a bare tmp_path with no src/ (graceful empty-tree pass, no mocking), while TypeCheckRule.test_clean_run_scores_100 patches mypy via _patch_infra/_patch_check_src_ok/_mock_mypy_full to simulate a genuinely clean type-check happy path. Different rule SUTs (duplication vs quality_rules.TypeCheckRule), different fixtures (raw empty dir vs monkeypatched mypy infra); folding would couple a no-src skip with a mocked-clean-run regression guard. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "129b4881db48"
reason = "Inter-file heterogeneous fix-hint case across two unrelated rules: FormattingRule.test_fix_hint_when_violations mocks ruff format --check (CompletedProcess rc=1) and asserts 'ruff format' is in result.fix_hint, while SecurityRule.test_fix_hint_provided mocks bandit JSON output (HIGH severity finding) and asserts a bandit-oriented fix_hint is provided. Different rule SUTs (quality_rules.FormattingRule vs security.SecurityRule), different mock infra (subprocess CompletedProcess vs MagicMock bandit JSON), different hint payloads. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "183f6f779af3"
reason = "Inter-file heterogeneous case: LintingRule.test_lint_details_has_issues_key asserts `details['issues']` is a list on a clean package (lint contract), while TypeCheckRule.test_typecheck_details_has_errors_key asserts `details['errors']` is a list on a typed package (type-check contract). Different rule SUTs (quality.LintingRule vs quality.TypeCheckRule), different details schemas (`issues` vs `errors`), different toolchains (ruff vs mypy). ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "d31070d4279f"
reason = "Inter-file heterogeneous case: find_venv.test_returns_none_when_no_venv asserts `find_venv` returns None when no `.venv` exists anywhere in the project tree (absence contract), while find_venv__max_venv_search_depth.test_find_venv_bounded_depth seeds a real `.venv` then asserts None when called from beyond `_MAX_VENV_SEARCH_DEPTH` levels deep (depth-bound contract). Same return value, opposite reasons — one guards the no-venv path, the other guards the search-depth limit boundary. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "baedb510dffb"
reason = "Heterogeneous intra-file case in test_file_naming.py: test_canonical_integration calls canonical_filename(symbols_or_tuples=tuple[str,...], tier='integration', single_binary=None) covering K=2/K=1/PascalCase→snake variants, while test_canonical_e2e_single_binary calls canonical_filename(symbols_or_tuples=list[tuple[str,str]], tier='e2e', single_binary='axm-audit') covering single-binary strip vs no-sub. Different tier branches, different `single_binary` mode, different input shapes. Folding would force a generic tier/single_binary parameter and erase the e2e-vs-integration distinction. ACK."


[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "6acb44ecc5f5"
reason = "Both tests exercise flatten_class but guard orthogonal concerns: test_flatten_class_to_top_level_funcs asserts the body-flattening contract (methods test_a/test_b become top-level functions, 'class TestX' is gone) on a no-decorator input, while test_flatten_preserves_method_decorators asserts the decorator-preservation invariant ('@pytest.mark.skip' survives flattening) on a decorated input. Different inputs (decorated vs not), different assertion targets (function bodies vs decorator survival); collapsing would erase the decorator-preservation branch."


# ── Fix-corpus fixture data (tests/fixtures/fix_corpus/**) — NOT real tests ──
# These files are static input/expected snapshots fed to the fix-pipeline
# integration tests; the dedup rule scans them as if they were the suite's own
# tests and clusters the (intentionally near-identical) input/expected pairs.
# False positives by construction — the corpus must contain these shapes.
# ── Inter-file 'same shape, different rule SUT/contract' — cross-module folds forbidden ──
[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "9787543367b9"
reason = "Inter-file cluster of 17 tests across 5 rule SUTs (DependencyHygieneRule, DocstringCoverageRule, FormattingRule, TypeCheckRule, and quality_rules unit). They share the `assert result.passed`/`result.details` literal shape but each targets a DIFFERENT rule's pass/detail contract. Folding cross-module would couple unrelated rule modules and erase per-rule coverage. Distinct-SUT — ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "b47b6584a57a"
reason = "Inter-file cluster of 4 tests across 3 SUTs (FormattingRule integration, quality_rules unit, ToolingRule unit). Shared `result.passed`/`result.details` literal shape but each pins a distinct rule's pass/detail contract on a different module. Folding cross-module would erase per-rule coverage. Distinct-SUT — ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "6a10eac48933"
reason = "Inter-file cluster of 4 tests across 2 SUTs (quality_rules check.text rendering vs formatters check-details renderers). Each guards a different rendered prefix/schema on a distinct module; no shared helper signature. Folding would erase per-renderer contracts. Distinct-SUT — ACK (same pattern as e2dcba1445ba)."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "b7bd09b75d85"
reason = "Inter-file e2e cluster pairs two different CLI sub-commands' contracts: `audit` (test_audit.py) and `fix` (test_fix.py), each a black-box subprocess invocation of a distinct binary path with its own expected exit/output. Different SUTs; folding would conflate two CLI commands. Distinct-SUT — ACK (same pattern as 4fe87554847a)."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "e33c9bdf87e6"
reason = "Inter-file integration cluster pairs `audit` (test_audit.py) and `test`/TestReport (test_cli_test__test_report.py) CLI sub-commands, each mocking a different upstream and asserting a distinct rendered prefix. Different SUTs (cli.audit vs cli.test); no shared helper. Distinct-SUT — ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "88beddac7a68"
reason = "Tests exercise two distinct public SUTs that share an empty-project boundary: extract_shared_helpers_once (single sweep) vs extract_shared_helpers (fixed-point loop); each needs its own mirror coverage and parametrizing over the callable would erase which function is verified. Distinct-SUT — ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "56c49f0b7bb4"
reason = "Tests guard two opposite fixed-point branches of pipeline.run apply-loop: converges_when_no_ops_planned uses the all-empty `neutralized` fixture and asserts iterations==1 (immediate convergence), while apply_loop_caps_at_max_iterations monkeypatches plan_relocate to never empty and asserts iterations==MAX_ITERATIONS (cap saturation). Different setup (shared fixture vs bespoke patch), different branch, different expected meaning; folding would erase the converge-vs-cap distinction. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "7d50db207ad4"
reason = "Both assert report.by_kind() but guard disjoint pipeline.run branches: records_relocate_ops_in_report feeds two relocate ops and asserts {'relocate': 2} (accumulation), while flatten_drops_pathological_ops feeds one actionable + one PATHOLOGICAL flatten op and asserts {'flatten': 1} (the pathological-filter branch drops one). Different code path (accumulate vs filter) and different expected; folding would erase the filtering contract. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "e8667545cfd1"
reason = "Cluster spans two distinct SUTs and disjoint guard branches: relocate_no_tests_dir_returns_empty + relocate_no_noncanonical_dirs_returns_empty guard relocate_non_canonical_tiers (no-tests-dir vs no-noncanonical-dirs early returns), while flatten_no_tests_dir_returns_empty + flatten_aggregates_messages_across_tiers guard flatten_tier_layout (no-tests-dir guard vs the message-aggregation path that returns a non-empty concatenated list). Mixed return-[] guards and an aggregate-non-empty assertion across two functions; folding would conflate unrelated SUTs. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "91a2c435ce46"
reason = "e2e fix tests guard opposite contracts: dry_run_on_clean_package_reports_no_ops drives the success path (returncode 0, '(no deterministic ops planned)' on stdout) while nonexistent_path_exits_1 drives the error path (returncode 1, 'Not a directory' on stderr). Different concern (success vs error-exit), different asserted stream and value; folding would erase the success-vs-failure distinction. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "fb860ddc1809"
reason = "file_depth_from_project tests assert two different contracts: returns_zero_for_path_outside_project pins a literal (== 0 for a path outside the project) while invariant_under_project_path asserts a relation (depth(f1, p1) == depth(f2, p2) — invariance under project-root depth). One is a boundary literal, the other a property/invariant; folding into a single expected value would erase the invariance check. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "1d2338f71acb"
reason = "Both guard pipeline.run atomic-rollback but via disjoint trigger branches: apply_rolls_back_on_failure monkeypatches _run_iterations to raise RuntimeError mid-apply (exception-propagation path, asserts pytest.raises(RuntimeError)) while apply_rejects_uncollectable_result monkeypatches _run_iterations to write a syntactically-broken test, exercising the post-apply pytest-collection gate (asserts a generic Exception). Different trigger (raised exception vs uncollectable-tree gate) and different exception type; folding would erase which rollback branch is covered. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "292e74040860"
reason = "Both assert plan_relocate(pkg) == [] but guard disjoint branches with disjoint setup: empty_when_clean uses a real clean make_pkg (no PYRAMID_LEVEL finding → no op, the no-findings path) while mixed_tiers_emits_zero_ops mocks _PLAN_CHECK to return conflicting unit/e2e tiers for one file, exercising the B3 mixed-tier oscillation guard (findings exist but are dropped). Same expected value, different reason/branch separately observable (one needs the mock); folding would erase the oscillation-guard coverage. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "ae588962177b"
reason = "Cluster of 15 disjoint cst_rewrite SUTs sharing only the _rewrite_and_dump helper: rename_function (in-place name swap, def + Name reference + marker-string rewrite), patch_file_depth +1/0/-1 and resolve-wrapper / non-positive / non-parents / slice / non-integer guards (parents[N] integer-literal increment/identity/decrement and refuse branches), flatten_class self-strip / docstring-drop / class-mark propagation / assignment-promotion, and dedupe shadow rule. Each test asserts on a different source-string contract (def rename, parents[N] index, decorator survival, import line) with literals that are SUT-specific data, not interchangeable parametrize values; folding would erase the per-rewriter contract. Successor of ex-26c967d93af4. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "5994b3fcc694"
reason = "Cluster of 8 disjoint cst_rewrite no-op / extension SUTs sharing only _rewrite_and_dump: backfill_import with dotted module path 'a.b.c' (asserts 'from a.b.c import helper' inserted), backfill at-top / after-future / already-imported / empty-mapping branches, flatten_class with unknown class name (class preserved untouched), and delete_function with absent target (module unchanged). Each test exercises a different rewriter and a different no-op-vs-extension contract; the literals (module path 'a.b.c', class 'TestY', function 'absent') are SUT-specific and not interchangeable parametrize values. Successor of ex-630bb3f2fec1. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "a2b2cb8532bd"
reason = "Cluster of 7 dedupe_imports tests over disjoint import-statement shapes: collapses plain `import os` (preserving `import sys`), collapses repeated `from pkg import foo` (preserving `from pkg import bar`), already-imported skip, star-import never collapsed, TYPE_CHECKING-block dedup, and emptied-TYPE_CHECKING-block removal. Same `lines.count(1)` literal applied to different source-string contracts (`import X` vs `from X import Y` vs `from x import *` vs nested TYPE_CHECKING); each pins a separate dedupe branch (Import vs ImportFrom vs star vs guarded block). Successor of ex-f64e47eadb17. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "9783c29e3cc8"
reason = "Inter-file cluster of 3 no-op tests across 3 distinct SUTs / files: rename_name_in_module (empty mapping), rename_top_level_in_source (empty mapping), reorder_module_statements (already-ordered). They share only the 'identity/no-op when nothing to do' shape but each targets a different public function with its own fixture and assertion. Cross-module fold forbidden; distinct-SUT, branches/contracts disjoint. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "f447f4b39d37"
reason = "Both call backfill_missing_imports but guard disjoint resolution branches: falls_back_to_project_index resolves `shared_helper` via a donor test file and asserts `from pkg import shared_helper` (project-index fallback path) while synthesizes_from_helpers resolves `make_widget` defined only in tests/<tier>/_helpers.py and asserts `import make_widget` (helpers-synthesis path). Different input setup, different resolution branch, different asserted import string; folding would erase which resolution path is covered. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "05eafab92751"
reason = "Inter-file cluster across 2 distinct SUTs: rename_name_in_module (renames a ClassDef) vs rename_top_level_in_source (renames the header only). They share a 'renames' shape but each pins a different public function's rename contract on a different module with its own fixture. Cross-module fold forbidden; distinct-SUT, branches/contracts disjoint. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "a085e1ec9174"
reason = "Both call delete_source_if_empty_tests and assert `not f.exists()`, but the assertion means opposite things on disjoint branches: unlinks_when_no_tests writes a file with no test funcs and verifies it was DELETED (deletion-effect path) while missing_file_is_noop passes a path that never existed and verifies the function is a safe NO-OP (absent-input guard). The coincidentally identical `not f.exists()` would conflate 'I deleted it' with 'it was never there'; folding would erase the delete-vs-noop distinction. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "c088412c671a"
reason = "Inter-file cluster across 2 distinct SUTs: strip_def_and_inject_import (no-match → injects nothing) vs strip_def_only (removes a function in place). Each test targets a different public function on a different file with its own contract (inject-nothing guard vs in-place removal). Cross-module fold forbidden; distinct-SUT, branches/contracts disjoint. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "cbcd53f53d1f"
reason = "Both call execute_split and assert `warnings == [...]` + `src.exists()`, but the warning literals encode two disjoint skip-reason branches: skipped_when_no_movable_units asserts 'split skipped: no movable units' (empty-source guard) while skipped_when_cohesive_single_group asserts 'has <2 unit-groups (file is cohesive at canonical-name level)' (single-group guard). Same assertion field but skip-reason-A vs skip-reason-B; folding into a generic expected message would erase which guard branch is covered. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "755c67e59f54"
reason = "Both call extract_shared_helpers_in_tier and assert `not _helpers.py.exists()` + `msgs == []`, but guard two disjoint skip branches with different inputs: constant_referencing_file_dunder_is_skipped feeds a `DATA_DIR = __file__` constant (location-bound skip) and asserts it stays, while single_occurrence_helper_is_not_extracted feeds a `_solo` helper present in one file only (len(files) < 2 guard) and asserts it stays. Skip-reason-A vs skip-reason-B with distinct asserted artefacts; folding would erase which guard is covered. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "04e0853d2857"
reason = "Inter-file cluster across 2 distinct SUTs / files: existing_helpers_module_is_loaded_not_overwritten (integration: a pre-existing _helpers.py is loaded, not clobbered) vs load_or_create_helpers_module_includes_future_import (unit: the created module carries the `from __future__` import). Different functions, different pyramid tiers, different fixtures and contracts (load-not-overwrite vs create-with-future). Cross-module/cross-tier fold forbidden; distinct-SUT, branches/contracts disjoint. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "9a66eb8aa92c"
reason = "Two distinct cst_rewrite SUTs sharing only _rewrite_and_dump: dedupe_imports_leaves_non_import_statements asserts non-import statements (`x = 1`, `def f`) survive dedup (preservation contract of dedupe_imports) while rename_function_ignores_non_matching_string asserts an unrelated string literal (the 'unrelated literal' constant) is left intact during a rename (preservation contract of rename_function). Different functions, different preserved artefacts; folding would conflate two unrelated rewriters. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "32e94713c1ee"
reason = "MirrorRule exempt_tests tests guard distinct branches with different assertion targets: excludes_orphan asserts orphan==[] + exempt_tests population + passed, independent_from_exempt_paths asserts missing still contains the source AND orphan cleared (forward/reverse independence), and no_exempt_tests_key_is_noop asserts orphan still flagged + exempt_tests==[]; parametrization would force a generic expected and erase the branch each documents."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "1cc0d58a848c"
reason = "Both call normalize_findings but guard opposite branches: findings_normalizes_entries (itself parametrized over dict / model_dump / vars / mixed entry shapes) asserts a NON-EMPTY normalized list equals the expected entries (the normalization path), while findings_returns_empty_for_no_findings asserts `== []` for empty / None / no-key inputs (the no-findings fallback). The assertion sense diverges (normalized entries vs empty list); merging the two parametrize tables would conflate the normalization contract with the empty-fallback contract. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "a5b2332c9ca7"
reason = "Cluster of 7 get_rules_for_category tests asserting disjoint contracts: all-categories count == 31, registry-derived count equality (no_manual_rule_enumeration), exact per-category count (category_filter_unchanged, parametrized), per-category len >= min (get_rules_by_category, parametrized), monkeypatched-registry surfacing (picks_up_registrations), ValueError on invalid category (raises path), and list/structure shape (empty_ok isinstance + hasattr check). Each probes a different facet (exact count vs lower bound vs registry derivation vs exception vs type), so a single parametrize would lie about the assertion target. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "28a28e764327"
reason = "Cluster of 4 run_bandit tests guarding disjoint return/log paths by bandit returncode: rc1+empty-stdout asserts {} AND a WARNING is logged (degraded path), rc1+valid-json asserts the parsed results dict AND no WARNING (recoverable path), rc0+empty asserts {} AND no WARNING (clean path), and rc2 asserts pytest.raises(RuntimeError) carrying the stderr (hard-failure path). The assertion targets diverge across return value, log presence, and exception, so folding would conflate warn/no-warn/raise contracts. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "658aac0d27ad"
reason = "Two DuplicateTestsRule non-clustering hardening tests guarding distinct SUT-disambiguation paths: distinct_self_attr_sut_not_clustered (parametrized over setUp / setup_method / pytest.fixture) verifies that two tests reading different self.parser_a/self.parser_b attributes set up in a class fixture are NOT clustered, while different_sut_sharing_result_assert_not_fused verifies that two same-named cross-file tests exercising different first-party symbols (AlphaRule vs BetaRule) sharing generic `result.passed`/`result.details` are NOT fused. Different fixture topology (intra-class self-attr divergence vs cross-file distinct symbol) and different clustering branch; the already-parametrized variant cannot absorb the bespoke cross-file case without erasing the second hardening path. ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "ba47c7510834"
reason = "Inter-file cluster of 15 tests across 7 distinct rule SUTs (BareExceptRule, BlockingIORule, CouplingMetricRule, DocstringCoverageRule, GodClassRule, MirrorRule, SecurityPatternRule). They share only the `assert result.passed is True` literal shape but each targets a different rule's pass contract with its own fixture/source layout; the same-file subsets (coupling x5, mirror x3, godclass x2, blocking_io x2) probe disjoint scenarios (per-module-override vs reads-pyproject vs low-coupling vs warnings-only vs error) with different setups and assertion fields, so they are not homogeneous parametrize candidates. Cross-rule folding would couple unrelated rule modules. Distinct-SUT — ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "85ded6d3fbfb"
reason = "Inter-file cluster of 15 tests across 9 files / 8+ rule SUTs (BlockingIORule, CouplingMetricRule, DuplicationRule, LintingRule, MirrorRule, PyprojectCompletenessRule, SecurityPatternRule, TypeCheckRule, quality_rules unit). They share only the `assert result.text is None` clean/passed shape but each targets a different rule's no-text contract with its own fixture (monkeypatched run_in_project vs real src layout vs distinct-bodies vs missing-src). The same-file subsets probe disjoint branches (duplication: all-unique vs 2-distinct vs no-src-dir; linting: monkeypatched-clean vs no-crash-with-passed-assert), not homogeneous parametrize candidates. Cross-rule folding would couple unrelated rule modules. Distinct-SUT — ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "ca50721fafe4"
reason = "Inter-file cluster of 8 audit_project/auditor tests with the ambiguous_raises_divergence false-positive signal (asymmetric pytest.raises across members). They guard disjoint contracts: three edge-case-survival tests (broken-lint, no-tests-dir, syntax-error) each assert isinstance(quality_score, float) on a different fixture; returns_audit_result asserts the AuditResult shape; runs_all_categories asserts category coverage; safe_check_isolation monkeypatches SecurityRule.check to raise and asserts the failure is recorded not propagated; crashing_rule_penalizes_score asserts crashed_rules + score penalty; nonexistent_path_raises_error (unit) asserts pytest.raises(FileNotFoundError). Different fixtures, assertion targets and branches; no shared helper signature across the 5 files. Distinct-branch — ACK."

[[tool.axm-audit.duplicate_tests.acknowledged]]
hash = "96bfe2bbbc39"
reason = "Inter-file cluster of 7 path/text-rendering tests across 6 files / 6 distinct rule SUTs (CircularImportRule FQN-prefix strip + arrow, DuplicationRule clone-pair func-name, LintingRule out-of-project path fallback, MirrorRule private-module-listing + no-truncation, TypeCheckRule src/ strip, DeadCodeRule src/ strip). They share only the 'rendered text contains/strips a path fragment' shape but each targets a different rule's text schema with its own fixture (real src I/O vs monkeypatched ruff JSON vs mocked mypy JSON vs mocked axm-ast subprocess JSON); the two MirrorRule members guard disjoint rendering branches (private-module listing vs single-missing no-truncation). Cross-rule folding would couple unrelated rule modules and erase per-rule text contracts. Distinct-SUT — ACK."

[tool.mypy]
python_version = "3.12"
strict = true
disallow_any_generics = true
disallow_any_unimported = true
disallow_any_explicit = true
warn_return_any = true
warn_unused_ignores = true
disallow_untyped_defs = true
show_error_codes = true
pretty = true
disallow_incomplete_defs = true
check_untyped_defs = true
plugins = ["pydantic.mypy"]
# fix-corpus input/expected trees are fixture mini-packages that reference
# modules only resolvable inside a per-test temp dir — skip type analysis.
exclude = ["tests/fixtures/fix_corpus/[^/]+/(input|expected)/"]

[[tool.mypy.overrides]]
module = ["tests.*"]
disallow_untyped_defs = false
disallow_incomplete_defs = false
disallow_any_explicit = false
disable_error_code = [
    "index",
    "arg-type",
    "attr-defined",
    "operator",
    "union-attr",
    "call-overload",
]

[[tool.mypy.overrides]]
module = ["_registry_helpers"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = ["radon.*"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = ["axm_anvil.*"]
ignore_missing_imports = true

# core/fix/ is relocated CST-based test reorganiser code: libcst's API and the
# Python ast module both surface untyped values that explode mypy strict-mode
# (no-any-unimported, no-any-explicit, no-any-return). Relax strictness here
# rather than scatter inline `type: ignore`s — same pragmatic posture used by
# axm_audit.core.rules.quality_rules for untyped third-party calls.
[[tool.mypy.overrides]]
module = ["axm_audit.core.fix.*"]
disallow_any_unimported = false
disallow_any_explicit = false
disallow_any_generics = false
warn_return_any = false
disallow_untyped_calls = false
disable_error_code = [
    "no-any-return",
    "no-any-unimported",
    "misc",
    "arg-type",
    "attr-defined",
    "assignment",
    "unused-ignore",
    "type-arg",
    "operator",
    "call-overload",
    "return-value",
    "union-attr",
]

[[tool.mypy.overrides]]
module = ["axm_audit.core.rules.quality_rules"]
disallow_untyped_calls = false


[[tool.mypy.overrides]]
module = ["axm_audit.tools.*"]
disallow_subclassing_any = false

[[tool.mypy.overrides]]
module = ["axm_audit.cli"]
disallow_untyped_decorators = false

# ─────────────────────────────────────────────────────────────────────────────
# Pytest Configuration
# ─────────────────────────────────────────────────────────────────────────────
[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"
addopts = [
    "-v",
    "--strict-markers",
    "--strict-config",
    "--tb=short",
    "--cov=src/axm_audit",
    "--cov-report=term-missing",
    "--cov-report=xml",
    "--import-mode=importlib",
]
pythonpath = ["src", "tests"]
filterwarnings = ["ignore::DeprecationWarning"]
asyncio_mode = "auto"
markers = [
    "integration: integration tests (real I/O, filesystem, DB, network)",
    "e2e: end-to-end tests (subprocess, CLI black box)",
    "slow: long-running tests (opt-in via -m slow)",
    "tautology_ok: opt out of TEST_QUALITY_TAUTOLOGY (optional reason arg)",
    "no_package_symbol_ok: opt out of TEST_QUALITY_NO_PACKAGE_SYMBOL (legitimate script-shim e2e)",
]

# ─────────────────────────────────────────────────────────────────────────────
# Coverage Configuration
# ─────────────────────────────────────────────────────────────────────────────
[tool.coverage.run]
source = ["src"]
branch = true
relative_files = true
omit = ["*/tests/*"]

[tool.coverage.xml]
output = "coverage.xml"

[tool.coverage.report]
exclude_lines = ["pragma: no cover", "if TYPE_CHECKING:", "raise NotImplementedError"]

# ─────────────────────────────────────────────────────────────────────────────
# Git-Cliff Configuration (Changelog Generation)
# ─────────────────────────────────────────────────────────────────────────────
[tool.git-cliff.changelog]
header = """
# Changelog

All notable changes to this project will be documented in this file.
"""
body = """
{% if version %}\
    ## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}\
    ## [Unreleased]
{% endif %}\
{% for group, commits in commits | group_by(attribute="group") %}
    ### {{ group | striptags | trim | upper_first }}
    {% for commit in commits %}
        - {{ commit.message | upper_first }}\
    {% endfor %}
{% endfor %}
"""
trim = true

[tool.git-cliff.git]
conventional_commits = true
filter_unconventional = true
commit_parsers = [
    { message = "^chore", skip = true },
    { message = "^ci", skip = true },
    { message = "^feat", group = "🚀 Features" },
    { message = "^fix", group = "🐛 Bug Fixes" },
    { message = "^doc", group = "📚 Documentation" },
    { message = "^perf", group = "⚡ Performance" },
    { message = "^refactor", group = "🔧 Refactor" },
    { message = "^test", group = "🧪 Tests" },
]
tag_pattern = "audit/v[0-9].*"
include_path = "packages/axm-audit/**"
